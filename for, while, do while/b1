#include <stdio.h>
int main(){
    int sum=0,b;
    for(int i=0; i<10;i++){
        printf("nhap so thu %d:",i+1);
        scanf("%d",&b);
        sum+=b;
    }
    printf("Tong cac so tren la: %d",sum);
}
